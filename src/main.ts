const x: number = 2;
const y: string = "Chad";
let a = false;
a = true;
const isAlive: boolean = true;

const result = addTwoNumbers(5, 8);
const result2: string = addTwoNumbers(2, 3);

function addTwoNumbers(a: number, b: number): string {
    return `${a + b}`;
}